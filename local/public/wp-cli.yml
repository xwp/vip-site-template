# Update the `setup` script in package.json to configure the correct type of site.
url: local.wpenv.net
path: wp

# Single site config.
core install:
  skip-config: true
  title: VIP Go
  admin_user: devgo
  admin_password: devgo
  admin_email: local@devgo.vip

# Multisite config.
core multisite-install:
  skip-config: true
  subdomains: true
  title: VIP Go
  admin_user: devgo
  admin_password: devgo
  admin_email: local@devgo.vip

@test:
  url: test.local.wpenv.net
  path: wp

@devgo:
  url: local.wpenv.net
  path: wp
