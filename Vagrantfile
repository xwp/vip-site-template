Vagrant.configure("2") do |config|
	config.vm.hostname = "vipgo" # Change this to match your project name.
end

load File.join(
	File.dirname(__FILE__),
	"public/plugins/vendor/wpsh/local/Vagrantfile"
)
